spring.application.name=shopease

# --- BASE DE DONNEES ---
# Si vous êtes en local, utilisez localhost
spring.datasource.url=jdbc:postgresql://localhost:5432/shopease
spring.datasource.username=postgres
# ? REMPLACEZ CECI PAR VOTRE VRAI MOT DE PASSE POSTGRESQL ?
spring.datasource.password=monMotDePasse123 
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA/Hibernate properties
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# --- MAIL ---
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=shopease.platform@gmail.com
# ? Mettez un mot de passe ou laissez vide pour tester le démarrage ?
spring.mail.password=monMotDePasseGmailApp
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# --- JWT (Sécurité) ---
jwt.auth.app=authapp
# ? J'ai mis une fausse clé ici pour que ça démarre. Ne mettez pas ${...} ?
jwt.auth.secret_key=MaCleSecreteSuperLonguePourLeDeveloppementLocal123456
jwt.auth.expires_in=3600

# --- GOOGLE SIGNIN (Valeurs fictives pour démarrer) ---
spring.security.oauth2.client.registration.google.client-id=test-client-id
spring.security.oauth2.client.registration.google.client-secret=test-client-secret
spring.security.oauth2.client.registration.google.scope=profile, email
spring.security.oauth2.client.registration.google.redirect-uri={baseUrl}/login/oauth2/code/google
spring.security.oauth2.client.registration.google.authorization-grant-type=authorization_code
spring.security.oauth2.client.registration.google.client-name=Google

spring.security.oauth2.client.provider.google.authorization-uri=https://accounts.google.com/o/oauth2/auth
spring.security.oauth2.client.provider.google.token-uri=https://oauth2.googleapis.com/token
spring.security.oauth2.client.provider.google.user-info-uri=https://www.googleapis.com/oauth2/v3/userinfo


# --- PAYMENT GATEWAY (STRIPE) ---
# ? C'est ici que ça plantait. J'ai mis "sk_test_..." en dur. ?
stripe.secret=sk_test_MaFausseCleStripePourLeDemarrage

# --- FILE UPLOAD ---
FILE_ZONE=test-zone
FILE_UPLOAD_API_KEY=test-key
FILE_UPLOAD_HOST_URL=http://localhost:8080